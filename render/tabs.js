(function() {
  // -------------------------------------------------------
  // TAB NAVIGATION
  // Note: renderTab, switchCategory, selectCategory, activateTab,
  // and switchTab are defined in core/actions.js and exposed to window there.
  // This module exists for load-order consistency.
  // -------------------------------------------------------
})();
